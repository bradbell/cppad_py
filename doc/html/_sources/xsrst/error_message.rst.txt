!!!!!!!!!!!!!
error_message
!!!!!!!!!!!!!

.. include:: ../preamble.rst

.. meta::
   :keywords: error_message, exception, handling

.. index:: error_message, exception, handling

.. _error_message:

Exception Handling
##################
- :ref:`error_message.syntax`
- :ref:`error_message.in_try_block`
    - :ref:`error_message.in_try_block.exception`
- :ref:`error_message.in_catch_block`
- :ref:`error_message.cppad_errors`
- :ref:`error_message.not_thread_safe`
- :ref:`error_message.example`

.. meta::
   :keywords: syntax

.. index:: syntax

.. _error_message.syntax:

Syntax
******
*stored_message* =  ``error_message`` ( *input_message* )

.. meta::
   :keywords: in, try, block

.. index:: in, try, block

.. _error_message.in_try_block:

In Try Block
************
If *input_message* is **not** the empty string,
it is stored in ``error_message`` and an exception is thrown.
This is intended to be done inside a ``try`` block.

.. meta::
   :keywords: exception

.. index:: exception

.. _error_message.in_try_block.exception:

exception
=========
The type of the exception is ``std::runtime_error``
which is derived from ``std::exception`` .
If the standard exception ``what()`` is called,
the return value will be the value of *input_message*
when the exception was thrown.

.. meta::
   :keywords: in, catch, block

.. index:: in, catch, block

.. _error_message.in_catch_block:

In Catch Block
**************
If *input_message* is the empty string,
the most recently stored message in ``error_message`` is returned.
In addition, the message is deleted from ``error_message`` .
If there are no more messages stored in ``error_message`` ,
the empty string is returned.
This is intended to be done inside a ``catch`` block.

.. meta::
   :keywords: cppad, errors

.. index:: cppad, errors

.. _error_message.cppad_errors:

Cppad Errors
************
Calls to the Cppad error handler get mapped to a call
to ``error_message`` .

.. meta::
   :keywords: not, thread, safe

.. index:: not, thread, safe

.. _error_message.not_thread_safe:

Not Thread Safe
***************
The message storage is done using static information in
``error_message`` and hence is not thread safe.

.. toctree::
   :maxdepth: 1
   :hidden:

   error_message_xam_cpp
   error_message_xam_py

.. meta::
   :keywords: example

.. index:: example

.. _error_message.example:

Example
*******
:ref:`c++<error_message_xam_cpp>`,
:ref:`python<error_message_xam_py>`.

----

xsrst input file: ``lib/cplusplus/error.cpp``
